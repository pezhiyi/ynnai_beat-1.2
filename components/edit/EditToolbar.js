import { useState } from 'react';
import { AnimatePresence } from 'framer-motion';

const EditToolbar = ({ selectedLayer }) => {
  return (
    <div className="toolbar">
      {/* 其他工具按钮可以放在这里 */}
    </div>
  );
};

export default EditToolbar; 